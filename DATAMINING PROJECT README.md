# 유기동물 입양 가능성 예측 및 입양 취약 동물 후보 선정

서울시 유기동물 데이터를 기반으로 개별 동물의 입양 여부를 예측하고, 입양 가능성이 낮은 동물을 조기에 찾아 추가 홍보·지원 대상 후보로 선정하기 위한 데이터마이닝 프로젝트입니다.

본 프로젝트는 단순히 “입양될 동물”을 맞히는 것보다, 보호소 자원을 더 효과적으로 배분하고 입양 취약 동물에게 더 많은 노출과 지원을 제공하는 것을 목표로 합니다.

---

## 1. 프로젝트 개요

매년 많은 유기동물이 구조되지만 모든 동물이 동일한 확률로 입양되지는 않습니다. 품종, 나이, 체중, 성별, 중성화 여부, 색상, 특징 설명과 같은 여러 요인이 함께 작용하기 때문에 직관만으로 입양 가능성을 판단하기 어렵습니다.

이 프로젝트에서는 유기동물의 구조적 정보와 텍스트 설명을 함께 사용하여 입양 여부를 예측하는 이진 분류 모델을 구축했습니다.

### 주요 목표

- 서울시 유기동물 데이터 기반 입양 여부 예측
- 입양 가능성이 낮은 동물 조기 식별
- 입양 취약 동물에 대한 홍보 및 지원 우선순위 설정
- Baseline 모델과의 비교를 통한 데이터마이닝 활용 효과 검증
- TF-IDF 기반 특징 설명 텍스트의 예측 기여도 확인

---

## 2. 사용 데이터

### 데이터 출처

- 농림축산식품부 농림축산검역본부 국가동물보호정보시스템
- 구조동물조회서비스 Open API
- 데이터 링크: https://www.data.go.kr/data/15098931/openapi.do

### 데이터 범위

- 기간: 2024~2025년
- 지역: 서울특별시
- 보호소: 한국동물구조관리협회
- 대상 동물: 개, 고양이
- 최초 확보 샘플 수: 5,161개
- 최종 학습 샘플 수: 3,997개

### 타깃 정의

| 상태 | 처리 방식 | 입양여부 |
|---|---:|---:|
| 종료(입양) | 사용 | 1 |
| 종료(자연사) | 사용 | 0 |
| 종료(안락사) | 사용 | 0 |
| 종료(반환) | 제외 | - |
| 보호중 | 제외 | - |

`종료(반환)`은 원주인에게 돌아간 사례이므로 “새 가정으로 입양되었는가”라는 문제 정의와 성격이 달라 제외했습니다. `보호중`은 결과가 확정되지 않아 학습 대상에서 제외했습니다.

---

## 3. 프로젝트 구조

```text
.
├── adoption_prediction_final.ipynb   # 데이터 전처리, 모델 학습, 평가, 예측 함수가 포함된 노트북
├── abandoned_animals.xlsx            # 유기동물 원본 데이터 파일
└── README.md                         # 프로젝트 설명 문서
```

> `abandoned_animals.xlsx`는 노트북과 같은 경로에 있어야 합니다.

---

## 4. 실행 환경

본 프로젝트는 Python 기반 Jupyter Notebook에서 실행됩니다.

### 주요 라이브러리

```bash
pip install pandas numpy scikit-learn matplotlib openpyxl
```

### 사용 패키지

- `pandas`
- `numpy`
- `re`
- `scikit-learn`
  - `train_test_split`
  - `Pipeline`
  - `ColumnTransformer`
  - `OneHotEncoder`
  - `StandardScaler`
  - `TfidfVectorizer`
  - `LogisticRegression`
  - `accuracy_score`
  - `roc_auc_score`
  - `classification_report`
- `matplotlib`

---

## 5. 실행 방법

1. 저장소 또는 프로젝트 폴더를 준비합니다.

2. 데이터 파일을 노트북과 같은 위치에 둡니다.

```text
abandoned_animals.xlsx
```

3. Jupyter Notebook을 실행합니다.

```bash
jupyter notebook
```

4. `adoption_prediction_final.ipynb`를 열고 위에서부터 순서대로 실행합니다.

---

## 6. 전처리 과정

원본 데이터는 문자열 형태의 정보가 많기 때문에 모델 학습 전에 다음과 같은 전처리를 수행했습니다.

### 수치형 변수 처리

- `나이`: `2016(년생)` 형태에서 출생연도를 추출한 뒤, 기준연도 2025년에서 차감하여 연령 계산
- `무게`: `8.5(Kg)` 형태에서 숫자만 추출하여 `무게_kg` 생성
- `설명길이`: `특징설명` 문자열 길이 계산

### 범주형 변수 처리

- `품종`: 숫자 코드처럼 보이지만 범주형 정보이므로 문자열로 변환
- `성별`, `중성화여부`, `품종`: One-Hot Encoding 적용

### 텍스트 변수 처리

`특징설명`은 TF-IDF 방식으로 벡터화했습니다.

특징 설명 예시:

```text
온순. 얌전. 사람 좋아함. 치석. 털 상태 양호.
```

한국어 형태소 분석기 대신, 데이터의 작성 형식에 맞춰 마침표(`.`)와 쉼표(`,`)를 기준으로 구절 단위 토큰화를 수행했습니다.

```python
def kor_tokenizer(text):
    return [t.strip() for t in re.split(r"[.,]", str(text)) if t.strip()]
```

TF-IDF 설정은 다음과 같습니다.

```python
TfidfVectorizer(
    tokenizer=kor_tokenizer,
    min_df=3,
    max_features=500
)
```

- `min_df=3`: 3개 미만 문서에서 등장한 희귀 토큰 제거
- `max_features=500`: 상위 500개 토큰만 사용
- TF-IDF는 train 데이터에만 `fit`하고 test 데이터에는 `transform`만 적용하여 데이터 누수를 방지했습니다.

---

## 7. Train/Test 분할

전체 데이터를 75:25 비율로 분할했습니다.

```python
train_test_split(
    X,
    y,
    test_size=0.25,
    random_state=42,
    stratify=y
)
```

- Train: 2,997개
- Test: 1,000개
- Train 입양률: 0.272
- Test 입양률: 0.272

`stratify=y`를 사용해 train/test의 입양 여부 비율을 동일하게 유지했습니다.

---

## 8. 모델 구성

모든 모델은 Logistic Regression을 사용했으며, feature 구성만 다르게 하여 비교했습니다.

### 8.1 Main Model

구조적 feature와 특징 설명 TF-IDF를 함께 사용합니다.

| Feature 유형 | 사용 변수 |
|---|---|
| 수치형 | 연령, 무게_kg |
| 범주형 | 성별, 중성화여부, 품종 |
| 텍스트 | 특징설명 TF-IDF |

### 8.2 Baseline 1

품종 정보만 사용합니다.

| Feature 유형 | 사용 변수 |
|---|---|
| 범주형 | 품종 |

### 8.3 Baseline 2

Main Model과 동일한 구조적 feature를 사용하되, TF-IDF 대신 특징 설명의 길이만 사용합니다.

| Feature 유형 | 사용 변수 |
|---|---|
| 수치형 | 연령, 무게_kg, 설명길이 |
| 범주형 | 성별, 중성화여부, 품종 |

Baseline 2는 TF-IDF의 효과를 확인하기 위한 모델입니다.

---

## 9. 학습 방식

개와 고양이는 입양 패턴과 품종 구조가 다를 수 있으므로, 종별로 별도 모델을 학습했습니다.

```text
개 데이터 → 개 전용 모델 학습
고양이 데이터 → 고양이 전용 모델 학습
```

예측 단계에서는 각 instance의 `동물종류`에 맞는 모델을 선택하여 입양 확률을 계산합니다.

---

## 10. 모델 성능

전체 test set 기준 성능은 다음과 같습니다.

| 모델 | Accuracy | AUC |
|---|---:|---:|
| Main Model (TF-IDF) | 0.7830 | 0.7942 |
| Baseline 1 (품종) | 0.7590 | 0.7082 |
| Baseline 2 (설명길이) | 0.7770 | 0.7445 |
| 다수 클래스 기준선 | 0.7280 | 0.5000 |

전체 데이터에서 미입양 비율이 높아 단순히 모든 동물을 “입양 아님”으로 예측해도 약 72.8%의 정확도를 얻을 수 있습니다. 따라서 Accuracy만으로는 모델의 실제 구분력을 판단하기 어렵기 때문에 AUC를 함께 사용했습니다.

Main Model은 Accuracy와 AUC 모두에서 Baseline 모델보다 높은 성능을 보였으며, 특히 Baseline 2보다 높은 AUC를 기록하여 TF-IDF 기반 텍스트 정보가 단순 설명 길이보다 유의미한 예측 정보를 제공함을 확인했습니다.

### Main Model 상세 분류 성능

| 클래스 | Precision | Recall | F1-score | Support |
|---|---:|---:|---:|---:|
| 입양아님 | 0.80 | 0.93 | 0.86 | 728 |
| 입양 | 0.68 | 0.39 | 0.49 | 272 |
| Accuracy |  |  | 0.78 | 1000 |

---

## 11. 신규 동물 입양 확률 예측

학습된 종별 Main Model을 사용해 새로운 유기동물 instance의 입양 확률을 예측할 수 있습니다.

```python
demo_good = {
    "동물종류": "개",
    "품종": "000114",
    "나이": "2023(년생)",
    "무게": "5(Kg)",
    "성별": "M",
    "중성화여부": "Y",
    "특징설명": "온순. 활발. 사람 좋아함. 잘 따름. 털 상태 양호.",
}
```

예측 결과 예시:

| 예시 | 설명 | 입양 확률 |
|---|---|---:|
| demo_good | 젊고 온순하며 사람을 좋아하는 개 | 92.2% |
| demo_bad | 노령, 질환, 경계성 특징이 있는 개 | 18.6% |

입양 확률이 일정 기준 이하인 동물은 입양 취약 후보로 선정하여 홍보 강화, 의료 지원, 프로필 개선 등 추가 지원 대상으로 활용할 수 있습니다.

---

## 12. 활용 방안

이 모델은 입양 가능성이 낮은 동물을 배제하기 위한 도구가 아니라, 오히려 더 많은 홍보와 지원을 제공하기 위한 우선순위 도구로 활용되어야 합니다.

### 기대 효과

- 입양 취약 동물 조기 식별
- 보호소 자원 배분 효율화
- 추가 홍보 및 지원 대상 선정
- 입양률 향상을 위한 데이터 기반 의사결정 지원

---

## 13. 한계점

### 지역적 한계

서울특별시와 특정 보호소 데이터를 중심으로 학습했기 때문에 다른 지역이나 다른 보호소에 그대로 적용했을 때 동일한 성능을 보장하기 어렵습니다.

### 변수의 한계

입양 여부에는 사진 품질, 홍보 방식, 보호 기간, 보호소 접근성, 입양자의 선호, 생활 환경 등 다양한 요인이 영향을 줄 수 있으나 본 프로젝트에서는 제한된 변수만 사용했습니다.

### 텍스트 표현의 한계

TF-IDF는 단어 또는 구절의 빈도를 기반으로 하므로 문장의 의미, 맥락, 뉘앙스를 완전히 반영하기 어렵습니다.

### 클래스 불균형

입양 데이터보다 미입양 데이터가 더 많아 입양 클래스의 recall이 상대적으로 낮게 나타났습니다. 향후 threshold 조정, class weight 적용, 재표본추출 등을 검토할 수 있습니다.

---


## 14. 프로젝트 결론

본 프로젝트에서는 유기동물의 구조적 정보와 특징 설명 텍스트를 함께 사용해 입양 여부를 예측하는 Logistic Regression 기반 모델을 구축했습니다.

Main Model은 품종만 사용하는 Baseline 1과 설명 길이만 사용하는 Baseline 2보다 높은 Accuracy와 AUC를 기록했습니다. 이를 통해 유기동물의 특징 설명을 TF-IDF로 반영하는 것이 입양 가능성 예측에 유의미하게 기여함을 확인했습니다.

최종적으로 본 모델은 입양 가능성이 낮은 동물을 조기에 식별하고, 해당 동물에게 더 많은 홍보와 지원을 제공하기 위한 데이터 기반 의사결정 도구로 활용될 수 있습니다.
